{
  "name": "miditool-e2e",
  "private": true,
  "description": "Playwright suite for the miditool web remote",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^24.0.0"
  }
}
